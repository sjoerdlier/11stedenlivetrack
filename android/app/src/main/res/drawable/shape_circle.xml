<?xml version="1.0" encoding="utf-8"?>
<!-- A plain filled circle, recolored at runtime via backgroundTintList
     (see MainActivity's pulse animation) — one shared shape for both pulse
     rings and the solid core, rather than baking a fixed color in here. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/brand" />
</shape>
